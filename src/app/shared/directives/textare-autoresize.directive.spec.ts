import { TextareAutoresizeDirective } from './textare-autoresize.directive';

describe('TextareAutoresizeDirective', () => {
  it('should create an instance', () => {
    const directive = new TextareAutoresizeDirective();
    expect(directive).toBeTruthy();
  });
});
